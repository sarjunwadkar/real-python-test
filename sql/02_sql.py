import sqlite3

# conn = sqlite3.connect("new.db")

# cursor = conn.cursor()

# cursor.execute("""INSERT INTO population values('New York', 'NY', 84000)""")

# cursor.execute("""INSERT INTO population values('London', 'GB', 95000)""")

# conn.commit()

# conn.close()

# # no explicit commit and close is required when using with with clause 
# with sqlite3.connect("new.db") as conn :
#   cursor = conn.cursor()
#   cursor.execute("INSERT INTO population values('Manchester','GB',45000)")
#   cursor.execute("INSERT INTO population values('Pune','IN',73000)")

# use of executemany


  